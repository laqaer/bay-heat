import type { Fact, Source } from "../types/evidence.ts";

// Lane stub (W9: seal and insulate). Populate with facts for bottom-seal profiles, door-kit U-values,
// weatherstrip packages, etc. lib/facts/index.ts merges this into the site-wide registry.
export const FACTS: Fact[] = [];
export const SOURCES: Record<string, Source> = {};
