import type { Product } from "../types.ts";

// Lane stub (W6: safety and lab). CO alarms, extinguishers, freeze alarms -- the only products ever linked
// next to a NO-GO verdict (as "safer alternatives"), never the NO-GO product itself.
export const PRODUCTS: Product[] = [];
